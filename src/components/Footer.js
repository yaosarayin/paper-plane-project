import React from "react"
import Navbar from '../components/Navbar'


export default function Footer({ children }) {
  return (
    <div style={{ textAlign: 'center', height: '100px' , display: 'flex', flexDirection: 'column', justifyContent: 'center'}}>
      Paper Plane Project
    </div>
  )
}